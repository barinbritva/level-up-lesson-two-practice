{
  "name": "green-fair",
  "version": "1.0.0",
  "scripts": {
    "tsc-build": "rm -rf ./public/js && tsc --outDir ./public/js",
    "clean-build": "rm -rf ./public/js",
    "build": "npm run clean-build && NODE_ENV=production webpack",
    "build:dev": "npm run clean-build && NODE_ENV=development webpack"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "ts-loader": "^8.0.11",
    "typescript": "^4.1.2",
    "webpack": "^5.9.0",
    "webpack-cli": "^4.2.0"
  }
}
